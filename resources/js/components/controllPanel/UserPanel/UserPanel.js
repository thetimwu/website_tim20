import React from "react";
import Register from "./Register";

const UserPanel = () => {
    return (
        <div style={{ marginTop: 200 }}>
            <Register />
        </div>
    );
};

export default UserPanel;
